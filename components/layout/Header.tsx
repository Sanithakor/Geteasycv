/**
 * Header.tsx — re-exports Navigation for backwards compatibility.
 * All new code should import Navigation directly from @/components/Navigation.
 */
export { default } from '@/components/Navigation';
